import { Component } from '@angular/core';

let module: any;

@Component({
    selector: '<%= shortName %>-main',
    moduleId: module.id,
    templateUrl: 'main.component.html',
    directives: [
      // directives
    ],
    providers: [
      // providers
    ]
})

export class MainComponent {
  constructor() { }
}
